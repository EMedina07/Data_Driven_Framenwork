package frameworkUtilities;

import frameworkActions.DriverActions;
import org.openqa.selenium.chrome.ChromeDriver;

public class GoogleChrome extends DriverActions {
    public GoogleChrome(ChromeDriver driver){
        super(driver);
    }
}
